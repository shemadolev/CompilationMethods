int fib(num : int);

void main(){
    a : int;
    read(a);

    write("Fibonnaci value is ");
    write(fib(a));
    write("\n");
}

int fib(n : int){
    if(n <= 1) return 1;
    return fib(n - 1) + fib(n - 2);
}

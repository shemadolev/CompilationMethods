#int sum(a:float,b:int){
#    return (int)a + b;
#}

int sum(a,b:int){
	write("sum arg is: ");
	write(a);
	write("\n");    
	return a + a;
}


void main(){
    a: int;
    b : float;
    read(a);
    read(b);

    intSum:int;
    floatSum:float;

    a = (int)((float)a);
    b = (float)((int)b);

    a = (int)a;
    b = (float)b;

    intSum = sum(a,a);
    write(",");
write(sum(a,a));
    write("\n");
}
